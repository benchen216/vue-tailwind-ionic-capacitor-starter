import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-tailwind-ionic-capacitor-starter',
  webDir: 'dist'
};

export default config;
